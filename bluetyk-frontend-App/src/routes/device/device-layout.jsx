import { createFileRoute } from '@tanstack/react-router';
import DeviceLayout from '../../pages/Device/DeviceLayout.jsx';

export const Route = createFileRoute('/device/device-layout')({
  component: DeviceLayout,
})


